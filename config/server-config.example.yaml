# Smidr Control Plane / Server Configuration
server:
  host: 0.0.0.0
  port: 8080
  tls: false  # Set to true and add cert_file/key_file for HTTPS

database:
  type: sqlite
  path: /var/lib/smidr/smidr.db

logging:
  level: info
  format: json  # or text
