# Smidr UI Environment Configuration

# Control plane API base URL
# Default: https://localhost:5001
# For Docker: This is baked into the build at build-time
# Docker compose will set this via build args to point to the control-plane service
VITE_API_BASE_URL=https://localhost:5001
